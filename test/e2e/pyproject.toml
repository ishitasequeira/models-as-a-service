[tool.pytest.ini_options]
markers = [
    "serial: cluster-wide mutations; excluded from parallel pass 1 (see prow two-phase pytest)",
    "slow: long-running test (may wait for token expiry)",
]
addopts = "--strict-markers"
